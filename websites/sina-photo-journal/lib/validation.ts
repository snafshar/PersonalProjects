import {z} from 'zod';
import {categories} from './content.ts';
export const photoInput=z.object({title:z.string().trim().min(1).max(140),caption:z.string().trim().min(1).max(12000),alt:z.string().trim().min(3).max(500),category:z.string().refine(v=>categories.includes(v)),location:z.string().trim().max(150).default(''),camera:z.string().trim().max(100).default(''),lens:z.string().trim().max(100).default(''),settings:z.string().trim().max(180).default(''),price_cents:z.number().int().min(0).max(500000).refine(n=>n===0||n>=100),published:z.number().int().min(0).max(1),width:z.number().int().min(1).max(50000),height:z.number().int().min(1).max(50000),rights:z.literal(true)});
export const presetInput=z.object({name:z.string().trim().min(1).max(100),data:z.object({scenarioId:z.string().max(100),gear:z.object({cameraName:z.string().max(100),lensName:z.string().max(100),sensor:z.enum(['full-frame','aps-c','micro-four-thirds','one-inch']),focalLength:z.number().min(4).max(2000),maxAperture:z.number().min(.7).max(32),stabilizationStops:z.number().min(0).max(8),hasTripod:z.boolean(),flashAvailable:z.boolean()}),context:z.object({light:z.enum(['bright','overcast','golden','indoor','low','night']),motion:z.enum(['still','slow','fast','erratic']),intent:z.enum(['freeze','balanced','motion']),handheld:z.boolean(),flashAllowed:z.boolean(),output:z.enum(['social','standard','large-print'])})})});
export function detectedImage(bytes:Uint8Array){if(bytes[0]===255&&bytes[1]===216&&bytes[2]===255)return 'image/jpeg';if(bytes[0]===137&&bytes[1]===80&&bytes[2]===78&&bytes[3]===71&&bytes[4]===13&&bytes[5]===10&&bytes[6]===26&&bytes[7]===10)return 'image/png';if(new TextDecoder().decode(bytes.slice(0,4))==='RIFF'&&new TextDecoder().decode(bytes.slice(8,12))==='WEBP')return 'image/webp';return null}
export function safeFilename(name:string){return name.replace(/[^a-zA-Z0-9._-]/g,'_').slice(-120)||'photograph.jpg'}
export function handheldStable(shutterSeconds:number,focalLength:number,crop=1,stops=0){return shutterSeconds<=2**stops/(Math.max(1,focalLength)*crop)}
